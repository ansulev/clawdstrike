$schema: "https://moonrepo.dev/schemas/tasks.json"

fileGroups:
  sources:
    - "src/**/*"
    - "lib/**/*"
  tests:
    - "tests/**/*"
    - "**/*.test.ts"
    - "**/*.test.tsx"
    - "**/*.spec.ts"
  configs:
    - "*.config.*"
    - "tsconfig*.json"
    - "package.json"
    - "pyproject.toml"
  rust-sources:
    - "crates/**/*.rs"
    - "Cargo.toml"
    - "Cargo.lock"
  rulesets:
    - "rulesets/**/*.yaml"
    - "rulesets/**/*.yml"
