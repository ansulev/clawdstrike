---
phase: 04-intelligence-participation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workbench/src/lib/plugins/enrichment-type-registry.ts
  - apps/workbench/src/lib/plugins/__tests__/enrichment-type-registry.test.ts
  - apps/workbench/src/lib/workbench/pivot-enrichment.ts
  - apps/workbench/src/lib/workbench/__tests__/pivot-enrichment.test.ts
  - apps/workbench/src/components/workbench/findings/enrichment-sidebar.tsx
  - apps/workbench/src/components/workbench/findings/related-indicators-section.tsx
  - packages/sdk/plugin-sdk/src/types.ts
  - packages/sdk/plugin-sdk/src/context.ts
  - apps/workbench/src/lib/plugins/plugin-loader.ts
autonomous: true
requirements:
  - ADV-01
  - ADV-03

must_haves:
  truths:
    - "When a source returns relatedIndicators in its EnrichmentResult, those indicators appear in a Related Indicators section in the enrichment sidebar"
    - "Each related indicator has a one-click Enrich button that triggers enrichment for that indicator"
    - "A plugin can register a custom React renderer for a specific enrichment type, and the sidebar uses it instead of GenericContent"
    - "The EnrichmentTypeRegistry follows the same Map + snapshot + listeners pattern as ViewRegistry"
  artifacts:
    - path: "apps/workbench/src/lib/plugins/enrichment-type-registry.ts"
      provides: "EnrichmentTypeRegistry with register/get/useRenderer pattern"
      exports: ["registerEnrichmentRenderer", "getEnrichmentRenderer", "useEnrichmentRenderer", "enrichmentTypeRegistry"]
    - path: "apps/workbench/src/lib/workbench/pivot-enrichment.ts"
      provides: "extractRelatedIndicators and triggerPivotEnrichment functions"
      exports: ["extractRelatedIndicators", "triggerPivotEnrichment"]
    - path: "apps/workbench/src/components/workbench/findings/related-indicators-section.tsx"
      provides: "RelatedIndicatorsSection component with one-click enrich"
      exports: ["RelatedIndicatorsSection"]
  key_links:
    - from: "apps/workbench/src/components/workbench/findings/enrichment-sidebar.tsx"
      to: "apps/workbench/src/lib/plugins/enrichment-type-registry.ts"
      via: "useEnrichmentRenderer hook in EnrichmentContent"
      pattern: "useEnrichmentRenderer.*enrichment\\.type"
    - from: "apps/workbench/src/components/workbench/findings/enrichment-sidebar.tsx"
      to: "apps/workbench/src/components/workbench/findings/related-indicators-section.tsx"
      via: "RelatedIndicatorsSection rendered when relatedIndicators present"
      pattern: "RelatedIndicatorsSection"
    - from: "apps/workbench/src/lib/plugins/plugin-loader.ts"
      to: "apps/workbench/src/lib/plugins/enrichment-type-registry.ts"
      via: "routeContributions routes enrichmentRenderers to registry"
      pattern: "registerEnrichmentRenderer"
---

<objective>
Pivot enrichment and custom enrichment type renderers.

Purpose: Enable recursive threat graph exploration by surfacing related indicators from enrichment results with one-click follow-on enrichment, and allow plugins to register custom React renderers for their enrichment data instead of the generic key-value fallback.

Output: EnrichmentTypeRegistry module, pivot enrichment logic, RelatedIndicatorsSection component, updated EnrichmentSidebar with custom renderer support, SDK types for enrichment renderer contribution point.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-intelligence-participation/04-CONTEXT.md

@apps/workbench/src/lib/plugins/view-registry.ts
@apps/workbench/src/lib/plugins/plugin-loader.ts
@apps/workbench/src/components/workbench/findings/enrichment-sidebar.tsx
@apps/workbench/src/lib/workbench/finding-engine.ts
@packages/sdk/plugin-sdk/src/types.ts
@packages/sdk/plugin-sdk/src/context.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From apps/workbench/src/lib/workbench/finding-engine.ts:
```typescript
export interface Enrichment {
  id: string;
  type: "mitre_attack" | "ioc_extraction" | "spider_sense" | "external_feed"
    | "swarm_corroboration" | "reputation" | "geolocation" | "whois" | "custom";
  label: string;
  data: Record<string, unknown>;
  addedAt: number;
  source: string;
}

export interface Finding {
  id: string;
  enrichments: Enrichment[];
  // ... other fields
}
```

From apps/workbench/src/lib/plugins/view-registry.ts (pattern to follow):
```typescript
const viewMap = new Map<string, ViewRegistration>();
const listeners = new Set<() => void>();
let snapshotBySlot = new Map<ViewSlot, ViewRegistration[]>();

export function registerView(reg: ViewRegistration): () => void;
export function getView(id: string): ViewRegistration | undefined;
export function useViewsBySlot(slot: ViewSlot): ViewRegistration[];
```

From packages/sdk/plugin-sdk/src/types.ts:
```typescript
export interface PluginContributions {
  threatIntelSources?: ThreatIntelSourceContribution[];
  // ... other contribution types
}
```

From packages/sdk/plugin-sdk/src/context.ts:
```typescript
export interface PluginContext {
  pluginId: string;
  subscriptions: Disposable[];
  commands: CommandsApi;
  views: ViewsApi;
  // ... other APIs
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EnrichmentTypeRegistry and pivot enrichment logic</name>
  <files>
    apps/workbench/src/lib/plugins/enrichment-type-registry.ts,
    apps/workbench/src/lib/plugins/__tests__/enrichment-type-registry.test.ts,
    apps/workbench/src/lib/workbench/pivot-enrichment.ts,
    apps/workbench/src/lib/workbench/__tests__/pivot-enrichment.test.ts,
    packages/sdk/plugin-sdk/src/types.ts,
    packages/sdk/plugin-sdk/src/context.ts
  </files>
  <read_first>
    apps/workbench/src/lib/plugins/view-registry.ts,
    apps/workbench/src/lib/plugins/__tests__/view-registry.test.ts,
    apps/workbench/src/lib/workbench/finding-engine.ts,
    packages/sdk/plugin-sdk/src/types.ts,
    packages/sdk/plugin-sdk/src/context.ts
  </read_first>
  <behavior>
    EnrichmentTypeRegistry:
    - registerEnrichmentRenderer("virustotal", VTRenderer) stores the mapping and notifies listeners
    - registerEnrichmentRenderer("virustotal", AnotherRenderer) when "virustotal" already registered throws Error
    - getEnrichmentRenderer("virustotal") returns VTRenderer after registration
    - getEnrichmentRenderer("unknown") returns undefined
    - The dispose function returned by registerEnrichmentRenderer removes the renderer and notifies
    - useEnrichmentRenderer("virustotal") returns the registered component (useSyncExternalStore)

    Pivot enrichment:
    - extractRelatedIndicators(enrichments) with enrichment.data.relatedIndicators = [{type:"ip", value:"1.2.3.4"}] returns [{type:"ip", value:"1.2.3.4", sourceEnrichmentId: enrichment.id}]
    - extractRelatedIndicators(enrichments) with no relatedIndicators returns []
    - extractRelatedIndicators deduplicates by (type, value) tuple
    - triggerPivotEnrichment(indicator, orchestrator) calls orchestrator.enrich(indicator) (integration contract)
  </behavior>
  <action>
    1. Create `enrichment-type-registry.ts` following the exact Map + snapshot + listeners pattern from `view-registry.ts`:
       - `EnrichmentRendererRegistration` interface: `{ type: string; component: ComponentType<EnrichmentRendererProps> }`
       - `EnrichmentRendererProps` interface: `{ enrichment: Enrichment; data: Record<string, unknown> }` (import Enrichment from finding-engine)
       - Module-level `rendererMap = new Map<string, EnrichmentRendererRegistration>()`
       - `registerEnrichmentRenderer(type, component)` returns dispose function, throws if duplicate
       - `getEnrichmentRenderer(type)` returns ComponentType or undefined
       - `useEnrichmentRenderer(type)` hook via useSyncExternalStore
       - `enrichmentTypeRegistry` convenience object with register/get/onChange

    2. Create `pivot-enrichment.ts`:
       - `RelatedIndicator` interface: `{ type: string; value: string; context?: string; sourceEnrichmentId: string }`
       - `extractRelatedIndicators(enrichments: Enrichment[])`: scans each enrichment's `data.relatedIndicators` array, maps to RelatedIndicator[], deduplicates by `${type}:${value}`
       - `triggerPivotEnrichment(indicator: RelatedIndicator, enrichFn: (type: string, value: string) => void)`: thin wrapper calling enrichFn

    3. Add to `packages/sdk/plugin-sdk/src/types.ts`:
       - `EnrichmentRendererContribution` interface: `{ type: string; entrypoint: string }`
       - Add `enrichmentRenderers?: EnrichmentRendererContribution[]` to `PluginContributions`

    4. Add to `packages/sdk/plugin-sdk/src/context.ts`:
       - `EnrichmentRenderersApi` interface: `{ register(type: string, component: ComponentType): Disposable }`
       - Add `enrichmentRenderers: EnrichmentRenderersApi` to `PluginContext`

    5. Write tests matching the behavior spec above. Use vitest. For the registry, mock a simple React component as `() => null`. For pivot enrichment, create test enrichments with and without relatedIndicators.
  </action>
  <verify>
    <automated>cd apps/workbench && npx vitest run src/lib/plugins/__tests__/enrichment-type-registry.test.ts src/lib/workbench/__tests__/pivot-enrichment.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "registerEnrichmentRenderer" apps/workbench/src/lib/plugins/enrichment-type-registry.ts
    - grep -q "getEnrichmentRenderer" apps/workbench/src/lib/plugins/enrichment-type-registry.ts
    - grep -q "useEnrichmentRenderer" apps/workbench/src/lib/plugins/enrichment-type-registry.ts
    - grep -q "useSyncExternalStore" apps/workbench/src/lib/plugins/enrichment-type-registry.ts
    - grep -q "extractRelatedIndicators" apps/workbench/src/lib/workbench/pivot-enrichment.ts
    - grep -q "RelatedIndicator" apps/workbench/src/lib/workbench/pivot-enrichment.ts
    - grep -q "EnrichmentRendererContribution" packages/sdk/plugin-sdk/src/types.ts
    - grep -q "enrichmentRenderers" packages/sdk/plugin-sdk/src/types.ts
    - grep -q "EnrichmentRenderersApi" packages/sdk/plugin-sdk/src/context.ts
  </acceptance_criteria>
  <done>
    EnrichmentTypeRegistry registers/unregisters custom renderers with duplicate detection. Pivot enrichment extracts related indicators from enrichment data and deduplicates them. SDK types include EnrichmentRendererContribution and EnrichmentRenderersApi. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire EnrichmentSidebar to use custom renderers and show related indicators</name>
  <files>
    apps/workbench/src/components/workbench/findings/enrichment-sidebar.tsx,
    apps/workbench/src/components/workbench/findings/related-indicators-section.tsx,
    apps/workbench/src/lib/plugins/plugin-loader.ts
  </files>
  <read_first>
    apps/workbench/src/components/workbench/findings/enrichment-sidebar.tsx,
    apps/workbench/src/lib/plugins/enrichment-type-registry.ts,
    apps/workbench/src/lib/workbench/pivot-enrichment.ts,
    apps/workbench/src/lib/plugins/plugin-loader.ts
  </read_first>
  <action>
    1. Create `related-indicators-section.tsx`:
       - Import `RelatedIndicator` from `pivot-enrichment.ts`
       - Props: `{ indicators: RelatedIndicator[]; onEnrich: (indicator: RelatedIndicator) => void }`
       - Render a collapsible section titled "Related Indicators" with count badge
       - Each indicator shows: type badge (colored by IOC_TYPE_COLORS from enrichment-sidebar), value (mono font, truncated), and a small "Enrich" button (IconFlask icon, gold accent matching existing Run Enrichment button style)
       - Use the same styling patterns as `EnrichmentSection` (border-b, px-4, py-2.5, text-[10px], bg-[#131721])
       - Empty state: don't render the section at all if indicators is empty

    2. Update `enrichment-sidebar.tsx`:
       - Import `useEnrichmentRenderer` from `enrichment-type-registry.ts`
       - Import `extractRelatedIndicators` from `pivot-enrichment.ts`
       - Import `RelatedIndicatorsSection` from `related-indicators-section.tsx`
       - In `EnrichmentContent`, before the switch statement, call `useEnrichmentRenderer(enrichment.type)`. If a custom renderer is returned, render it with `<CustomRenderer enrichment={enrichment} data={enrichment.data} />` instead of the switch-case fallback. The existing switch-case types (mitre_attack, ioc_extraction, etc.) remain as built-in defaults -- custom renderers override them.
       - Add `onPivotEnrich?: (type: string, value: string) => void` to `EnrichmentSidebarProps`
       - After the enrichment sections, compute `relatedIndicators = extractRelatedIndicators(enrichments)` and render `<RelatedIndicatorsSection indicators={relatedIndicators} onEnrich={(ind) => onPivotEnrich?.(ind.type, ind.value)} />` if indicators exist

    3. Update `plugin-loader.ts` `routeContributions`:
       - Add routing block for `contributions.enrichmentRenderers` (after the existing gutter/contextMenu blocks)
       - For each renderer contribution: resolve the entrypoint module via `this.resolveViewEntrypoint(renderer.entrypoint)`, wrap in `lazy()`, and call `registerEnrichmentRenderer(renderer.type, LazyComponent)`
       - Push the dispose function to `disposables`
       - Import `registerEnrichmentRenderer` from `../plugins/enrichment-type-registry`
  </action>
  <verify>
    <automated>cd apps/workbench && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useEnrichmentRenderer" apps/workbench/src/components/workbench/findings/enrichment-sidebar.tsx
    - grep -q "extractRelatedIndicators" apps/workbench/src/components/workbench/findings/enrichment-sidebar.tsx
    - grep -q "RelatedIndicatorsSection" apps/workbench/src/components/workbench/findings/enrichment-sidebar.tsx
    - grep -q "onPivotEnrich" apps/workbench/src/components/workbench/findings/enrichment-sidebar.tsx
    - grep -q "RelatedIndicatorsSection" apps/workbench/src/components/workbench/findings/related-indicators-section.tsx
    - grep -q "onEnrich" apps/workbench/src/components/workbench/findings/related-indicators-section.tsx
    - grep -q "enrichmentRenderers" apps/workbench/src/lib/plugins/plugin-loader.ts
    - grep -q "registerEnrichmentRenderer" apps/workbench/src/lib/plugins/plugin-loader.ts
  </acceptance_criteria>
  <done>
    EnrichmentSidebar checks EnrichmentTypeRegistry for custom renderers before falling back to built-in switch-case. Related indicators from enrichment results appear in a dedicated section with one-click enrich buttons. Plugin loader routes enrichmentRenderer contributions to the registry. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
- EnrichmentTypeRegistry follows Map + snapshot + listeners pattern (matches view-registry.ts)
- Custom renderer registered for a type overrides the generic fallback in EnrichmentContent
- Related indicators extracted from enrichment data appear in the sidebar
- Each related indicator has a clickable Enrich button that invokes onPivotEnrich
- Plugin loader routes enrichmentRenderers contributions
- SDK types export EnrichmentRendererContribution and EnrichmentRenderersApi
- All tests pass, TypeScript compiles clean
</verification>

<success_criteria>
- A plugin calling `ctx.enrichmentRenderers.register("virustotal", VTComponent)` causes the sidebar to render VTComponent for VirusTotal enrichments
- Enrichment results with `data.relatedIndicators` array surface as clickable indicators in the sidebar
- The enrichment-type-registry.test.ts and pivot-enrichment.test.ts suites pass
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-intelligence-participation/04-01-SUMMARY.md`
</output>
