---
phase: 02-cli-scaffolding
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - packages/cli/create-plugin/tests/scaffold.integration.test.ts
  - packages/cli/create-plugin/tests/flags.test.ts
  - packages/cli/create-plugin/tests/templates.test.ts
autonomous: true
requirements:
  - SCAF-07

must_haves:
  truths:
    - "An integration test scaffolds a project with each of the 6 template types and asserts the output files exist with correct content"
    - "Flag parsing test validates kebab-case name enforcement, type validation, and default contribution resolution"
    - "Template output test verifies each template produces source containing createPlugin and correct contribution types"
  artifacts:
    - path: "packages/cli/create-plugin/tests/scaffold.integration.test.ts"
      provides: "Integration test scaffolding all 6 template types"
      contains: "scaffoldProject"
    - path: "packages/cli/create-plugin/tests/flags.test.ts"
      provides: "Unit tests for CLI flag parsing"
      contains: "parseFlags"
    - path: "packages/cli/create-plugin/tests/templates.test.ts"
      provides: "Unit tests for template output correctness"
      contains: "getSourceTemplate"
  key_links:
    - from: "packages/cli/create-plugin/tests/scaffold.integration.test.ts"
      to: "packages/cli/create-plugin/src/engine.ts"
      via: "imports scaffoldProject"
      pattern: "scaffoldProject"
    - from: "packages/cli/create-plugin/tests/flags.test.ts"
      to: "packages/cli/create-plugin/src/flags.ts"
      via: "imports parseFlags"
      pattern: "parseFlags"
    - from: "packages/cli/create-plugin/tests/templates.test.ts"
      to: "packages/cli/create-plugin/src/templates/source.ts"
      via: "imports getSourceTemplate, getTestTemplate"
      pattern: "getSourceTemplate"
---

<objective>
Create the test suite for the create-plugin CLI: unit tests for flag parsing and template output, plus an integration test that scaffolds all 6 template types and verifies the generated files exist with correct structure and content.

Purpose: SCAF-07 requires a CI integration test that scaffolds, builds, and tests each template type. The unit tests ensure individual components work. Together they provide confidence that scaffolded projects are correct.
Output: Passing test suite runnable via `npm test --workspace=packages/cli/create-plugin`.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-cli-scaffolding/02-01-SUMMARY.md
@.planning/phases/02-cli-scaffolding/02-02-SUMMARY.md
@packages/cli/create-plugin/src/types.ts
@packages/cli/create-plugin/src/flags.ts
@packages/cli/create-plugin/src/engine.ts
@packages/cli/create-plugin/src/templates/source.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flag parsing and template output unit tests</name>
  <files>
    packages/cli/create-plugin/tests/flags.test.ts
    packages/cli/create-plugin/tests/templates.test.ts
  </files>
  <read_first>
    packages/cli/create-plugin/src/types.ts
    packages/cli/create-plugin/src/flags.ts
    packages/cli/create-plugin/src/templates/source.ts
    packages/cli/create-plugin/src/templates/guard.ts
    packages/cli/create-plugin/src/templates/test.ts
  </read_first>
  <behavior>
    Flag parsing (flags.test.ts):
    - parseFlags(["my-guard", "--type", "guard"]) returns ScaffoldOptions with name "my-guard", type "guard"
    - parseFlags(["my-guard", "--type", "guard", "--publisher", "acme"]) sets publisher to "acme"
    - parseFlags(["my-guard", "--type", "guard", "--contributions", "guards,commands"]) sets contributions array
    - parseFlags(["MyGuard", "--type", "guard"]) returns null (non-kebab-case name rejected)
    - parseFlags(["my_guard", "--type", "guard"]) returns null (underscores rejected)
    - parseFlags(["--type", "guard"]) returns null (missing name)
    - parseFlags(["my-guard"]) returns null (missing type)
    - parseFlags(["my-guard", "--type", "invalid"]) returns null (invalid type)
    - parseFlags(["my-guard", "--type", "guard"]) sets displayName to "My Guard" (derived)
    - parseFlags(["my-guard", "--type", "guard"]) sets contributions to PLUGIN_TYPE_DEFAULTS.guard

    Template output (templates.test.ts):
    - getSourceTemplate with type "guard" contains "createPlugin"
    - getSourceTemplate with type "guard" contains "GuardContribution" or "ctx.guards.register"
    - getSourceTemplate with type "detection" contains "FileTypeContribution" or "ctx.fileTypes.register"
    - getSourceTemplate with type "detection" contains "DetectionAdapterContribution"
    - getSourceTemplate with type "ui" contains "EditorTabContribution"
    - getSourceTemplate with type "intel" contains "ThreatIntelSourceContribution"
    - getSourceTemplate with type "compliance" contains "ComplianceFrameworkContribution"
    - getSourceTemplate with type "full" contains "ctx.guards.register"
    - getTestTemplate for any type contains "createSpyContext"
    - getTestTemplate for any type contains "plugin-sdk/testing"
    - getTestTemplate for type "guard" contains "guards.registered" or "assertContributions"
  </behavior>
  <action>
1. Create `packages/cli/create-plugin/tests/flags.test.ts`:
   - Import { describe, it, expect } from "vitest"
   - Import { parseFlags } from "../src/flags"
   - Import { PLUGIN_TYPE_DEFAULTS } from "../src/types"
   - Write test cases per the behavior spec above
   - Test group "valid inputs" for happy paths
   - Test group "validation" for rejection cases
   - Test group "defaults" for derived values (displayName, contributions, publisher)

2. Create `packages/cli/create-plugin/tests/templates.test.ts`:
   - Import { describe, it, expect } from "vitest"
   - Import { getSourceTemplate, getTestTemplate } from "../src/templates/source"
   - Import { ScaffoldOptions, PluginType } from "../src/types"
   - Create a helper `makeOptions(type: PluginType): ScaffoldOptions` with sensible defaults
   - Test group per template type ("guard template", "detection template", etc.)
   - Each group verifies the output string contains expected type references and createPlugin usage
   - Test group "test template" verifying createSpyContext import and plugin-sdk/testing path
   - Test "all 6 types produce non-empty output" using a loop over all PluginType values
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/clawdstrike/.claude/worktrees/plugin-ecosystem && npx vitest run --project packages/cli/create-plugin -- tests/flags.test.ts tests/templates.test.ts 2>&1 || cd /Users/connor/Medica/backbay/standalone/clawdstrike/.claude/worktrees/plugin-ecosystem && npx vitest run --config packages/cli/create-plugin/vitest.config.ts -- tests/flags.test.ts tests/templates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'parseFlags' packages/cli/create-plugin/tests/flags.test.ts
    - grep -q 'kebab' packages/cli/create-plugin/tests/flags.test.ts || grep -q 'MyGuard' packages/cli/create-plugin/tests/flags.test.ts
    - grep -q 'getSourceTemplate' packages/cli/create-plugin/tests/templates.test.ts
    - grep -q 'getTestTemplate' packages/cli/create-plugin/tests/templates.test.ts
    - grep -q 'createSpyContext' packages/cli/create-plugin/tests/templates.test.ts
    - grep -q 'guard' packages/cli/create-plugin/tests/templates.test.ts
    - grep -q 'detection' packages/cli/create-plugin/tests/templates.test.ts
    - grep -q 'full' packages/cli/create-plugin/tests/templates.test.ts
  </acceptance_criteria>
  <done>
    - Flag parsing tests cover valid inputs, validation rejections, and default derivation
    - Template tests verify each of the 6 types produces output containing expected SDK type references
    - Test template tests verify createSpyContext import and plugin-sdk/testing path
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold integration test</name>
  <files>
    packages/cli/create-plugin/tests/scaffold.integration.test.ts
  </files>
  <read_first>
    packages/cli/create-plugin/src/engine.ts
    packages/cli/create-plugin/src/types.ts
    packages/cli/create-plugin/src/templates/config.ts
  </read_first>
  <behavior>
    - scaffoldProject with type "guard" creates outputDir/package.json containing "@clawdstrike/plugin-sdk"
    - scaffoldProject with type "guard" creates outputDir/tsconfig.json containing "strict": true
    - scaffoldProject with type "guard" creates outputDir/tsup.config.ts
    - scaffoldProject with type "guard" creates outputDir/vitest.config.ts
    - scaffoldProject with type "guard" creates outputDir/.gitignore containing "node_modules"
    - scaffoldProject with type "guard" creates outputDir/src/index.ts containing "createPlugin"
    - scaffoldProject with type "guard" creates outputDir/tests/plugin.test.ts containing "createSpyContext"
    - All 6 template types produce a complete project directory
    - Generated package.json has build, test, typecheck scripts
    - Generated tsconfig.json uses ES2022 target and ESNext module
  </behavior>
  <action>
1. Create `packages/cli/create-plugin/tests/scaffold.integration.test.ts`:
   - Import { describe, it, expect, beforeAll, afterAll } from "vitest"
   - Import { scaffoldProject } from "../src/engine"
   - Import { ScaffoldOptions, PluginType } from "../src/types"
   - Import { existsSync, readFileSync, rmSync, mkdtempSync } from "node:fs"
   - Import { join } from "node:path"
   - Import { tmpdir } from "node:os"

   - Create a shared temp directory in beforeAll using mkdtempSync(join(tmpdir(), "clawdstrike-scaffold-"))
   - Clean up in afterAll using rmSync(tempDir, { recursive: true, force: true })

   - Define all 6 types: ["guard", "detection", "ui", "intel", "compliance", "full"] as const

   - For each type, create a describe block:
     a. Call scaffoldProject with a ScaffoldOptions using name `test-${type}-plugin`, the temp dir as parent, and the current type
     b. Test "creates package.json": assert file exists, parse JSON, verify name field, verify @clawdstrike/plugin-sdk in dependencies, verify scripts.build exists, verify scripts.test exists, verify scripts.typecheck exists
     c. Test "creates tsconfig.json": assert file exists, parse JSON, verify compilerOptions.strict is true, verify target is "ES2022", verify module is "ESNext"
     d. Test "creates tsup.config.ts": assert file exists
     e. Test "creates vitest.config.ts": assert file exists
     f. Test "creates .gitignore": assert file exists, verify contains "node_modules"
     g. Test "creates src/index.ts": assert file exists, verify contains "createPlugin"
     h. Test "creates tests/plugin.test.ts": assert file exists, verify contains "createSpyContext" or "plugin-sdk/testing"

   - Add a summary test "all 6 types scaffold successfully" that loops over types and asserts the output directory exists with at least 5 files each

   NOTE: This test does NOT run `npm install && npm run build && npm test` on the generated projects -- that would require the full @clawdstrike/plugin-sdk/testing sub-path export to exist (Phase 1 dependency). SCAF-07 says "a CI integration test scaffolds a project with each template type, runs npm install && npm run build && npm test, and asserts all pass." The scaffold-and-verify-files portion is done here. The full build+test CI step will be enabled after Phase 1 completes and the /testing sub-path is available. Add a commented-out test block labeled "// TODO: Enable after Phase 1 (testing harness) completes" that shows the npm install + build + test flow.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/clawdstrike/.claude/worktrees/plugin-ecosystem && npx vitest run --config packages/cli/create-plugin/vitest.config.ts -- tests/scaffold.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'scaffoldProject' packages/cli/create-plugin/tests/scaffold.integration.test.ts
    - grep -q 'guard' packages/cli/create-plugin/tests/scaffold.integration.test.ts
    - grep -q 'detection' packages/cli/create-plugin/tests/scaffold.integration.test.ts
    - grep -q 'compliance' packages/cli/create-plugin/tests/scaffold.integration.test.ts
    - grep -q 'intel' packages/cli/create-plugin/tests/scaffold.integration.test.ts
    - grep -q 'full' packages/cli/create-plugin/tests/scaffold.integration.test.ts
    - grep -q 'package.json' packages/cli/create-plugin/tests/scaffold.integration.test.ts
    - grep -q 'createPlugin' packages/cli/create-plugin/tests/scaffold.integration.test.ts
    - grep -q 'createSpyContext\|plugin-sdk/testing' packages/cli/create-plugin/tests/scaffold.integration.test.ts
    - grep -q 'TODO.*Phase 1\|testing harness' packages/cli/create-plugin/tests/scaffold.integration.test.ts
  </acceptance_criteria>
  <done>
    - Integration test scaffolds all 6 template types into temp directories
    - Each scaffolded project verified to have package.json, tsconfig.json, tsup.config.ts, vitest.config.ts, .gitignore, src/index.ts, tests/plugin.test.ts
    - File content assertions confirm SDK dependency, strict mode, createPlugin usage, testing imports
    - Full build+test CI step documented as TODO pending Phase 1 completion
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `npx vitest run --config packages/cli/create-plugin/vitest.config.ts` passes (all test files)
- Integration test creates temp directories, scaffolds all 6 types, and cleans up
- No test requires external network or installed dependencies in scaffolded projects
</verification>

<success_criteria>
- Complete test suite covers flag parsing, template output, and scaffold integration
- All 6 template types verified to produce correct file structures
- Tests are self-contained (use temp directories, clean up after)
- Full CI build+test step documented for post-Phase-1 enablement
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-scaffolding/02-03-SUMMARY.md`
</output>
