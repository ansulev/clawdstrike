---
phase: 02-sidebar-panels-editor-tabs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workbench/src/features/shared/posture-utils.ts
  - apps/workbench/src/components/workbench/home/home-page.tsx
  - apps/workbench/src/features/activity-bar/panels/heartbeat-panel.tsx
  - apps/workbench/src/features/activity-bar/panels/sentinel-panel.tsx
  - apps/workbench/src/features/activity-bar/panels/findings-panel.tsx
autonomous: true
requirements:
  - SIDE-03
  - SIDE-04
  - SIDE-05
  - SIDE-06

must_haves:
  truths:
    - "HeartbeatPanel shows a posture ring, stat counts (sentinels, findings, approvals, fleet), and quick links that call openApp"
    - "SentinelPanel shows a filterable sentinel list grouped by status with status dots, a create button, and clickable items that call openApp"
    - "FindingsPanel shows a filterable findings list with severity badges and a collapsible intel section, with clickable items that call openApp"
    - "Posture derivation logic is extracted to a shared utility used by both HomePage and HeartbeatPanel"
  artifacts:
    - path: "apps/workbench/src/features/shared/posture-utils.ts"
      provides: "derivePosture function, POSTURE_CONFIG constant, Posture type"
      exports: ["derivePosture", "POSTURE_CONFIG", "Posture"]
    - path: "apps/workbench/src/features/activity-bar/panels/heartbeat-panel.tsx"
      provides: "HeartbeatPanel component"
      exports: ["HeartbeatPanel"]
    - path: "apps/workbench/src/features/activity-bar/panels/sentinel-panel.tsx"
      provides: "SentinelPanel component"
      exports: ["SentinelPanel"]
    - path: "apps/workbench/src/features/activity-bar/panels/findings-panel.tsx"
      provides: "FindingsPanel component"
      exports: ["FindingsPanel"]
  key_links:
    - from: "heartbeat-panel.tsx"
      to: "posture-utils.ts"
      via: "import derivePosture, POSTURE_CONFIG"
      pattern: "import.*derivePosture.*from.*posture-utils"
    - from: "home-page.tsx"
      to: "posture-utils.ts"
      via: "import derivePosture, POSTURE_CONFIG (replaces inline definitions)"
      pattern: "import.*derivePosture.*from.*posture-utils"
    - from: "heartbeat-panel.tsx"
      to: "usePaneStore.getState().openApp"
      via: "quick link click handlers"
      pattern: "openApp\\("
    - from: "sentinel-panel.tsx"
      to: "useSentinelStore.use.sentinels"
      via: "store subscription"
      pattern: "useSentinelStore\\.use\\.sentinels"
    - from: "findings-panel.tsx"
      to: "useFindingStore.use.findings"
      via: "store subscription"
      pattern: "useFindingStore\\.use\\.findings"
---

<objective>
Extract shared posture utilities and build the first 4 sidebar panels (Heartbeat, Sentinel, Findings, Explorer wiring).

Purpose: These panels replace PlaceholderPanel stubs with real, store-connected sidebar content. HeartbeatPanel provides system-at-a-glance status. SentinelPanel and FindingsPanel give operators quick access to their primary security data. ExplorerPanel wiring (SIDE-06) is already done from Phase 1 and just needs confirmation.

Output: 3 new panel components + 1 shared utility module. All panels follow the ExplorerPanel layout pattern (section header, filter, ScrollArea, footer).
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sidebar-panels-editor-tabs/02-RESEARCH.md
@.planning/phases/02-sidebar-panels-editor-tabs/02-UI-SPEC.md

<interfaces>
<!-- Stores and utilities consumed by panels -->

From src/features/sentinels/stores/sentinel-store.tsx:
```typescript
// Uses createSelectors pattern:
// useSentinelStore.use.sentinels() returns Sentinel[]
```

From src/lib/workbench/sentinel-types.ts:
```typescript
export type SentinelStatus = "active" | "paused" | "retired";
export interface Sentinel {
  id: string;
  name: string;
  status: SentinelStatus;
  mode: string;
  // ... other fields
}
```

From src/features/findings/stores/finding-store.tsx:
```typescript
// useFindingStore.use.findings() returns Finding[]
```

From src/lib/workbench/finding-engine.ts:
```typescript
export type FindingSeverity = "critical" | "high" | "medium" | "low" | "info";
export type FindingStatus = "emerging" | "confirmed" | "dismissed" | "resolved";
export interface Finding {
  id: string;
  title: string;
  severity: FindingSeverity;
  status: FindingStatus;
  // ... other fields
}
```

From src/features/findings/stores/intel-store.tsx:
```typescript
// useIntelStore.use.localIntel() returns Intel[]
// useIntelStore.use.swarmIntel() returns Intel[]
```

From src/lib/workbench/sentinel-types.ts:
```typescript
export interface Intel {
  id: string;
  label: string;
  type: "rule" | "ioc" | "ttp";
  shareability: "local" | "public" | "swarm";
  // ... other fields
}
```

From src/lib/workbench/finding-constants.ts:
```typescript
export const SEVERITY_COLORS: Record<FindingSeverity, string>;
export const SEVERITY_LABELS_SHORT: Record<FindingSeverity, string>;
export const STATUS_CONFIG: Record<FindingStatus, { label: string; color: string }>;
```

From src/features/fleet/use-fleet-connection.ts:
```typescript
// useFleetConnectionStore.use.connection() returns FleetConnection
// useFleetConnectionStore.use.agents() returns AgentInfo[]
```

From src/features/policy/stores/multi-policy-store.ts:
```typescript
// useMultiPolicy (via useWorkbench) for activePolicy.guards
```

From src/components/workbench/home/home-page.tsx (to be extracted):
```typescript
type Posture = "nominal" | "attention" | "critical" | "offline";
function derivePosture(fleetConnected, criticalFindings, emergingFindings, enabledGuards): Posture;
const POSTURE_CONFIG: Record<Posture, { label, color, glow, breathMs, ringStroke }>;
```

From src/components/workbench/explorer/explorer-panel.tsx:
```typescript
// Panel layout pattern: section header -> filter -> ScrollArea -> footer
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract posture utilities and create HeartbeatPanel</name>
  <files>
    apps/workbench/src/features/shared/posture-utils.ts
    apps/workbench/src/components/workbench/home/home-page.tsx
    apps/workbench/src/features/activity-bar/panels/heartbeat-panel.tsx
  </files>
  <read_first>
    apps/workbench/src/components/workbench/home/home-page.tsx
    apps/workbench/src/features/activity-bar/components/sidebar-panel.tsx
    apps/workbench/src/components/workbench/explorer/explorer-panel.tsx
    apps/workbench/src/features/sentinels/stores/sentinel-store.tsx
    apps/workbench/src/features/findings/stores/finding-store.tsx
    apps/workbench/src/features/fleet/use-fleet-connection.ts
    apps/workbench/src/features/policy/stores/multi-policy-store.ts
    apps/workbench/src/lib/workbench/guard-registry.ts
    .planning/phases/02-sidebar-panels-editor-tabs/02-UI-SPEC.md
  </read_first>
  <action>
    **Step 1: Create `src/features/shared/posture-utils.ts`**

    Create the directory `src/features/shared/` if it doesn't exist.

    Extract from `home-page.tsx`:
    - `Posture` type: `"nominal" | "attention" | "critical" | "offline"`
    - `derivePosture` function (lines 34-43 of home-page.tsx): takes `(fleetConnected: boolean, criticalFindings: number, emergingFindings: number, enabledGuards: number)` returns `Posture`
    - `POSTURE_CONFIG` constant (lines 46-81 of home-page.tsx): `Record<Posture, { label: string; color: string; glow: string; breathMs: number; ringStroke: string }>`

    Export all three.

    **Step 2: Update `home-page.tsx` to import from posture-utils**

    Remove the inline `Posture` type, `derivePosture` function, and `POSTURE_CONFIG` constant from home-page.tsx.

    Add import: `import { type Posture, derivePosture, POSTURE_CONFIG } from "@/features/shared/posture-utils";`

    Everything else in home-page.tsx stays unchanged.

    **Step 3: Create HeartbeatPanel at `src/features/activity-bar/panels/heartbeat-panel.tsx`**

    Structure follows UI-SPEC HeartbeatPanel section:

    ```
    Panel header: "System Status" + refresh icon button
    ─────────────────────────────
    80px SVG Posture Ring (centered)
    Posture label below ring (9px JetBrains Mono semibold, posture color)
    ─────────────────────────────
    2x2 stat grid: SENTINELS | FINDINGS | APPROVALS | FLEET
    Each cell: 9px uppercase label (#6f7f9a) above 20px semibold value (#ece7dc)
    Cells separated by 1px #2d3240 borders
    ─────────────────────────────
    QUICK LINKS section header (10px, uppercase)
    > Mission Control   (onClick -> openApp("/missions", "Mission Control"))
    > Approvals          (onClick -> openApp("/approvals", "Approvals"))
    > Audit Log          (onClick -> openApp("/audit", "Audit"))
    > Receipts           (onClick -> openApp("/receipts", "Receipts"))
    ```

    Data wiring:
    - `const sentinels = useSentinelStore.use.sentinels();` -- use `.length` for count
    - `const findings = useFindingStore.use.findings();` -- use `.length` for count + derive critical/emerging counts
    - `const connection = useFleetConnectionStore.use.connection();` -- `connection.connected`, `connection.agentCount`
    - For posture derivation: call `derivePosture(connection.connected, criticalCount, emergingCount, enabledGuardCount)`. Get enabledGuardCount from the active policy guards (read how home-page.tsx does it -- look for how it accesses GUARD_REGISTRY or activePolicy.guards).
    - Approval count: display "---" (no approval store exists per research). The stat cell is still shown but with "---" as the value. This makes it a clickable link to open the approval queue.

    Posture ring: Simplified 80px SVG. Single circle with `strokeDasharray` to show coverage arc. Use `POSTURE_CONFIG[posture].ringStroke` for color, `.glow` for drop-shadow. No guard segment arcs (too small at 80px per UI-SPEC).

    Quick links: `<button>` elements (not `<a>`) calling `usePaneStore.getState().openApp(route, label)`. Style: 32px height, `px-3`, 11px JetBrains Mono, `#ece7dc/70`, `>` chevron prefix in `#6f7f9a`, hover bg `#131721/40`.

    Empty state: If sentinels.length === 0 AND findings.length === 0 AND !connection.connected, show "System Idle" heading and "No sentinels deployed. Create a sentinel to begin monitoring." body (from UI-SPEC copywriting contract).

    Wrap scrollable content in `<ScrollArea>` from `@/components/ui/scroll-area`.
  </action>
  <verify>
    <automated>cd apps/workbench && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File apps/workbench/src/features/shared/posture-utils.ts exists
    - grep "export type Posture" apps/workbench/src/features/shared/posture-utils.ts matches
    - grep "export function derivePosture" apps/workbench/src/features/shared/posture-utils.ts matches
    - grep "export const POSTURE_CONFIG" apps/workbench/src/features/shared/posture-utils.ts matches
    - grep 'import.*derivePosture.*from.*posture-utils' apps/workbench/src/components/workbench/home/home-page.tsx matches
    - grep -c "type Posture" apps/workbench/src/components/workbench/home/home-page.tsx returns 0 (removed inline definition)
    - File apps/workbench/src/features/activity-bar/panels/heartbeat-panel.tsx exists
    - grep "export function HeartbeatPanel" apps/workbench/src/features/activity-bar/panels/heartbeat-panel.tsx matches
    - grep "derivePosture" apps/workbench/src/features/activity-bar/panels/heartbeat-panel.tsx matches
    - grep "openApp" apps/workbench/src/features/activity-bar/panels/heartbeat-panel.tsx matches
    - grep "useSentinelStore" apps/workbench/src/features/activity-bar/panels/heartbeat-panel.tsx matches
    - grep "System Idle" apps/workbench/src/features/activity-bar/panels/heartbeat-panel.tsx matches (empty state)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Posture logic extracted to shared utility. HeartbeatPanel renders posture ring, stat counts, and quick links. home-page.tsx imports from shared utility instead of inlining.</done>
</task>

<task type="auto">
  <name>Task 2: Create SentinelPanel and FindingsPanel</name>
  <files>
    apps/workbench/src/features/activity-bar/panels/sentinel-panel.tsx
    apps/workbench/src/features/activity-bar/panels/findings-panel.tsx
  </files>
  <read_first>
    apps/workbench/src/features/sentinels/stores/sentinel-store.tsx
    apps/workbench/src/features/findings/stores/finding-store.tsx
    apps/workbench/src/features/findings/stores/intel-store.tsx
    apps/workbench/src/lib/workbench/sentinel-types.ts
    apps/workbench/src/lib/workbench/finding-engine.ts
    apps/workbench/src/lib/workbench/finding-constants.ts
    apps/workbench/src/components/workbench/explorer/explorer-panel.tsx
    apps/workbench/src/features/activity-bar/panels/heartbeat-panel.tsx
    .planning/phases/02-sidebar-panels-editor-tabs/02-UI-SPEC.md
  </read_first>
  <action>
    **Step 1: Create SentinelPanel at `src/features/activity-bar/panels/sentinel-panel.tsx`**

    Structure per UI-SPEC SentinelPanel section:

    ```
    Panel header: "Sentinels" + [+] create button + refresh icon
    ─────────────────────────────
    Filter input: IconSearch + "Filter sentinels..." placeholder
    ─────────────────────────────
    ACTIVE (N)    [collapsible]
      [dot] Perimeter Watcher        active
      [dot] Exfil Hunter             active
    PAUSED (N)    [collapsible]
      [dot] Compliance Liaison       paused
    RETIRED (N)   [collapsible]
      [dot] Legacy Rule              retired
    ─────────────────────────────
    N sentinels
    ```

    Data wiring:
    - `const sentinels = useSentinelStore.use.sentinels();`
    - `const [filter, setFilter] = useState("");`
    - Filter: `sentinels.filter(s => s.name.toLowerCase().includes(filter.toLowerCase()))` in useMemo with deps `[sentinels, filter]`.
    - Group by status: create a `groupByStatus(sentinels)` helper that returns `Record<SentinelStatus, Sentinel[]>` with order: active, paused, retired.
    - Group headers: 10px JetBrains Mono semibold, `#6f7f9a`, uppercase. Collapsible with local `useState<Set<string>>` tracking collapsed groups. 8px chevron icon rotates on collapse.
    - Status dot colors: active = `#4ade80`, paused = `#d4a84b`, retired = `#6f7f9a`.
    - List items: 32px height, `px-3`. Left: 6px status dot + 4px gap + sentinel name (11px JetBrains Mono, truncated). Right: mode label (9px, `#6f7f9a`).
    - Click item: `usePaneStore.getState().openApp(\`/sentinels/${sentinel.id}\`, sentinel.name)`.
    - Create button `[+]`: `IconPlus` size 12, ghost style. Click: `usePaneStore.getState().openApp("/sentinels/create", "New Sentinel")`.

    Filter input styling: 11px JetBrains Mono, `#ece7dc` text, `#0b0d13` bg, `#2d3240` border, `#d4a84b/40` focus border, 12px `IconSearch` on left, `pl-7 pr-2 py-1`.

    Empty states:
    - No sentinels at all: "No Sentinels" heading, "Create a sentinel to deploy autonomous threat monitoring." body, with a CTA button.
    - No matches for filter: "No sentinels match the current filter" body text.

    Wrap list in `<ScrollArea>`. Footer: `<span>` with `text-[9px] font-mono text-[#6f7f9a]/40` showing "{N} sentinels".

    **Step 2: Create FindingsPanel at `src/features/activity-bar/panels/findings-panel.tsx`**

    Structure per UI-SPEC FindingsPanel section:

    ```
    Panel header: "Findings & Intel" + refresh icon
    ─────────────────────────────
    Filter input: IconSearch + "Filter findings..." placeholder
    ─────────────────────────────
    FINDINGS (N)  [collapsible]
      [CRIT] Credential exfil attempt    emerging
      [HIGH] Lateral movement detected   confirmed
    - - - - - - - - - - - - - - (dashed divider)
    INTEL (N)     [collapsible]
      [rule] Exfil detection pattern     public
      [ioc]  C2 beacon indicators        swarm
    ─────────────────────────────
    N findings, M intel
    ```

    Data wiring:
    - `const findings = useFindingStore.use.findings();`
    - `const localIntel = useIntelStore.use.localIntel();`
    - `const swarmIntel = useIntelStore.use.swarmIntel();`
    - Combined intel: `[...localIntel, ...swarmIntel]` in useMemo.
    - `const [filter, setFilter] = useState("");`
    - Filter applies to both findings (by title) and intel (by label), case-insensitive.

    Finding list items: 32px height. Left: severity badge pill -- `SEVERITY_LABELS_SHORT[finding.severity]` text, colored using `SEVERITY_COLORS[finding.severity]` for text and `${SEVERITY_COLORS[finding.severity]}20` for background. Badge: `px-1.5 py-0.5 rounded text-[9px] font-mono font-semibold`, 16px height, `border-radius: 3px`. Center: finding title (11px JetBrains Mono, truncated). Right: status label using `STATUS_CONFIG[finding.status].label` with `STATUS_CONFIG[finding.status].color`.
    - Click finding: `usePaneStore.getState().openApp(\`/findings/${finding.id}\`, finding.title)`.

    Intel list items: 32px height. Left: type icon (9px, `#6f7f9a`) -- use text labels "rule"/"ioc"/"ttp". Center: intel label (11px JetBrains Mono, truncated). Right: shareability label (9px, `#6f7f9a`).
    - Click intel: `usePaneStore.getState().openApp(\`/intel/${intel.id}\`, intel.label)`.

    Section divider between findings and intel: 1px dashed border with `border-[#2d3240]/40`.

    Two collapsible sections: "FINDINGS" and "INTEL", independently collapsible with local state.

    Empty states:
    - No findings: "No Findings" heading, "Findings appear here when sentinels detect potential threats." body.
    - No filter matches: "No findings match the current filter" body.

    Footer: "{N} findings, {M} intel".
  </action>
  <verify>
    <automated>cd apps/workbench && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File apps/workbench/src/features/activity-bar/panels/sentinel-panel.tsx exists
    - grep "export function SentinelPanel" apps/workbench/src/features/activity-bar/panels/sentinel-panel.tsx matches
    - grep "useSentinelStore" apps/workbench/src/features/activity-bar/panels/sentinel-panel.tsx matches
    - grep "openApp" apps/workbench/src/features/activity-bar/panels/sentinel-panel.tsx matches
    - grep "No Sentinels" apps/workbench/src/features/activity-bar/panels/sentinel-panel.tsx matches (empty state)
    - grep "/sentinels/create" apps/workbench/src/features/activity-bar/panels/sentinel-panel.tsx matches (create button)
    - File apps/workbench/src/features/activity-bar/panels/findings-panel.tsx exists
    - grep "export function FindingsPanel" apps/workbench/src/features/activity-bar/panels/findings-panel.tsx matches
    - grep "useFindingStore" apps/workbench/src/features/activity-bar/panels/findings-panel.tsx matches
    - grep "useIntelStore" apps/workbench/src/features/activity-bar/panels/findings-panel.tsx matches
    - grep "SEVERITY_COLORS" apps/workbench/src/features/activity-bar/panels/findings-panel.tsx matches
    - grep "SEVERITY_LABELS_SHORT" apps/workbench/src/features/activity-bar/panels/findings-panel.tsx matches
    - grep "openApp" apps/workbench/src/features/activity-bar/panels/findings-panel.tsx matches
    - grep "No Findings" apps/workbench/src/features/activity-bar/panels/findings-panel.tsx matches (empty state)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>SentinelPanel renders filterable sentinel list grouped by status with create button. FindingsPanel renders findings with severity badges and collapsible intel section. Both panels call openApp on item click.</done>
</task>

</tasks>

<verification>
- `cd apps/workbench && npx tsc --noEmit` -- no type errors
- `cd apps/workbench && npx vitest run` -- all tests pass (including home-page if tested)
- posture-utils.ts exports derivePosture, POSTURE_CONFIG, Posture
- home-page.tsx imports from posture-utils (no inline Posture/derivePosture/POSTURE_CONFIG)
- All 3 panel files export named components
- All panels use openApp for navigation
- All panels use ScrollArea for content
- All panels have empty state copy matching UI-SPEC
</verification>

<success_criteria>
- HeartbeatPanel shows posture ring, 4 stat counts, and 4 quick links (SIDE-03)
- SentinelPanel shows filterable, grouped sentinel list with status dots and create button (SIDE-04)
- FindingsPanel shows findings with severity badges and collapsible intel section (SIDE-05)
- ExplorerPanel already integrated from Phase 1 (SIDE-06 satisfied)
- Posture logic shared between HomePage and HeartbeatPanel (no duplication)
</success_criteria>

<output>
After completion, create `.planning/phases/02-sidebar-panels-editor-tabs/02-02-SUMMARY.md`
</output>
