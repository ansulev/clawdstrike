---
phase: 05-layout-wiring-sdk-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/workbench/src/components/workbench/editor/policy-editor.tsx
  - apps/workbench/src/components/workbench/editor/policy-tab-bar.tsx
autonomous: true
requirements: [BPAN-01, BPAN-02, RSIDE-01, RSIDE-02, CTXM-03]
gap_closure: true

must_haves:
  truths:
    - "Plugin bottom panel tabs appear alongside Problems, Test Runner, Evidence Pack, and Explainability in the policy editor bottom panel"
    - "Plugin right sidebar panels appear alongside Version History, Evidence Pack, Explainability, and Publish in the editor right sidebar"
    - "Plugin context menu items appear in the tab right-click context menu with when-clause filtering"
  artifacts:
    - path: "apps/workbench/src/components/workbench/editor/policy-editor.tsx"
      provides: "BottomPanelTabs and RightSidebarPanels mounted in layout"
      contains: "BottomPanelTabs"
    - path: "apps/workbench/src/components/workbench/editor/policy-tab-bar.tsx"
      provides: "PluginContextMenuItems embedded in TabContextMenu"
      contains: "PluginContextMenuItems"
  key_links:
    - from: "apps/workbench/src/components/workbench/editor/policy-editor.tsx"
      to: "apps/workbench/src/components/workbench/editor/bottom-panel-tabs.tsx"
      via: "import and render BottomPanelTabs component"
      pattern: "import.*BottomPanelTabs.*from"
    - from: "apps/workbench/src/components/workbench/editor/policy-editor.tsx"
      to: "apps/workbench/src/components/workbench/editor/right-sidebar-panels.tsx"
      via: "import and render RightSidebarPanels component"
      pattern: "import.*RightSidebarPanels.*from"
    - from: "apps/workbench/src/components/workbench/editor/policy-tab-bar.tsx"
      to: "apps/workbench/src/components/plugins/plugin-context-menu.tsx"
      via: "import and render PluginContextMenuItems in TabContextMenu"
      pattern: "import.*PluginContextMenuItems.*from"
---

<objective>
Mount orphaned Phase 3 and Phase 4 components into the live app layout, closing the last 3 integration gaps in the v3.0 milestone.

Purpose: BottomPanelTabs, RightSidebarPanels, and PluginContextMenuItems were built and tested in Phases 3-4 but never wired into the running app. This plan mounts them in policy-editor.tsx and policy-tab-bar.tsx so plugins can actually contribute views to these slots at runtime.

Output: All 3 components rendered in the live workbench layout.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v3.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From apps/workbench/src/components/workbench/editor/bottom-panel-tabs.tsx:
```typescript
export interface BuiltInTab {
  id: string;
  label: string;
  icon: ComponentType;
  content: ReactNode;
}

interface BottomPanelTabsProps {
  builtInTabs: BuiltInTab[];
  panelHeight: number;
  activeTabId: string | null;
  onTabChange: (tabId: string) => void;
}

export function BottomPanelTabs({ builtInTabs, panelHeight, activeTabId, onTabChange }: BottomPanelTabsProps)
```

From apps/workbench/src/components/workbench/editor/right-sidebar-panels.tsx:
```typescript
export interface BuiltInPanel {
  id: string;
  label: string;
  icon: ComponentType;
  content: ReactNode;
}

interface RightSidebarPanelsProps {
  builtInPanels: BuiltInPanel[];
  sidebarWidth: number;
  activePanelId: string | null;
  onPanelChange: (panelId: string | null) => void;
}

// Returns a fragment: [panel content (if active)] + [icon strip]
export function RightSidebarPanels({ builtInPanels, sidebarWidth, activePanelId, onPanelChange }: RightSidebarPanelsProps)
```

From apps/workbench/src/components/plugins/plugin-context-menu.tsx:
```typescript
import { type ContextMenuTarget, type WhenContext } from "@/lib/plugins/context-menu-registry";

interface PluginContextMenuItemsProps {
  menu: ContextMenuTarget;        // "editor" | "sidebar" | "tab" | "finding" | "sentinel"
  context: WhenContext;           // Record<string, string | boolean | number>
  onExecuteCommand: (commandId: string) => void;
  className?: string;
}

export function PluginContextMenuItems({ menu, context, onExecuteCommand }: PluginContextMenuItemsProps)
```

From apps/workbench/src/components/workbench/editor/policy-editor.tsx (current bottom panel area, ~line 961-993):
The bottom panel is currently TestRunnerPanel inside a ResizablePanel. The right sidebar is currently a set of conditional 280px divs for historyOpen, evidenceOpen, explainOpen, publishOpen.

From apps/workbench/src/components/workbench/editor/policy-tab-bar.tsx (TabContextMenu ~line 32-105):
TabContextMenu receives menu (ContextMenuState with tabId/x/y), onClose, onCloseTab, onCloseOthers, onCloseAll, onDuplicate, onRename. It renders a fixed-position context menu with Close, Close Others, Close All, Duplicate, Rename items.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount BottomPanelTabs in policy-editor.tsx bottom panel area</name>
  <files>apps/workbench/src/components/workbench/editor/policy-editor.tsx</files>
  <read_first>
    - apps/workbench/src/components/workbench/editor/policy-editor.tsx (full file -- understand current bottom panel layout)
    - apps/workbench/src/components/workbench/editor/bottom-panel-tabs.tsx (BottomPanelTabs props interface)
  </read_first>
  <action>
Import BottomPanelTabs and BuiltInTab from "@/components/workbench/editor/bottom-panel-tabs".

Add a new state variable `bottomPanelActiveTab` (string | null, default "test-runner" when testRunnerOpen, null otherwise).

Replace the current bottom panel rendering (the ResizablePanel containing TestRunnerPanel at ~line 961-993) with BottomPanelTabs. The current bottom panel shows TestRunnerPanel inside a ResizablePanel when `testRunnerOpen` is true. The new approach:

1. When `testRunnerOpen` is true, render a ResizablePanelGroup with the editor on top and BottomPanelTabs on the bottom, passing:
   - `builtInTabs`: An array of BuiltInTab objects built with useMemo:
     - `{ id: "problems", label: "Problems", icon: IconAlertCircle, content: <ProblemsPanel diagnostics={problems} className="h-full" /> }`
     - `{ id: "test-runner", label: "Test Runner", icon: IconTestPipe, content: <TestRunnerPanel /> }`
     - `{ id: "evidence", label: "Evidence Pack", icon: IconPackage, content: <EvidencePackPanel documentId={activeTab?.documentId} fileType={activeTab?.fileType} /> }`
     - `{ id: "explainability", label: "Explainability", icon: IconBulb, content: <ExplainabilityPanel documentId={activeTab?.documentId} lastRun={labExecution.lastRun} baselineRun={labExecution.runHistory.length > 1 ? labExecution.runHistory[1] : null} onJumpToLine={(line) => window.dispatchEvent(new CustomEvent("workbench:jump-to-line", { detail: { line } }))} /> }`
   - `panelHeight={200}` (approximate ResizablePanel height; use a ref + ResizeObserver on the bottom panel for dynamic value, OR just pass 200 as a reasonable default since the ResizablePanel handles actual sizing)
   - `activeTabId={bottomPanelActiveTab}`
   - `onTabChange={(tabId) => setBottomPanelActiveTab(tabId)}`

2. Remove the standalone `showProblems` conditional rendering within the non-testRunner branch (lines 988-992) since Problems is now a tab in BottomPanelTabs. Instead, when `showProblems` is toggled, set `testRunnerOpen(true)` and `bottomPanelActiveTab("problems")`.

3. Similarly, update the `evidenceOpen`, `explainOpen` toolbar buttons to toggle the bottom panel with the appropriate active tab instead of rendering as separate right sidebar panels. This consolidates the bottom panel area.

IMPORTANT: Keep the existing behavior working -- the BottomPanelTabs component handles rendering the correct content for each tab. The key integration point is wrapping the built-in panel content nodes as BuiltInTab objects and passing them to BottomPanelTabs.

NOTE: The `showProblems` toggle button in the toolbar can be updated to: `setTestRunnerOpen(true); setBottomPanelActiveTab("problems");` on click. But keep it simple -- the primary goal is to get BottomPanelTabs mounted. If consolidating all bottom panels into BottomPanelTabs creates too much churn, an acceptable alternative is: keep TestRunnerPanel rendering as-is, but ADD BottomPanelTabs as a second tab bar that appears alongside it. The minimum viable approach: import BottomPanelTabs, construct builtInTabs with at minimum TestRunnerPanel, and render it where TestRunnerPanel currently renders.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/clawdstrike/.claude/worktrees/plugin-ecosystem && grep -n "BottomPanelTabs" apps/workbench/src/components/workbench/editor/policy-editor.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "import.*BottomPanelTabs" policy-editor.tsx returns a match
    - grep "builtInTabs" policy-editor.tsx returns at least one match (the prop being passed)
    - grep "<BottomPanelTabs" policy-editor.tsx returns at least one match (component rendered in JSX)
    - The BottomPanelTabs component receives builtInTabs, panelHeight, activeTabId, and onTabChange props
  </acceptance_criteria>
  <done>BottomPanelTabs is imported and rendered in policy-editor.tsx with built-in tabs (at minimum TestRunnerPanel) passed as builtInTabs prop. Plugin-contributed bottomPanelTab views will automatically appear via the ViewRegistry hook inside BottomPanelTabs.</done>
</task>

<task type="auto">
  <name>Task 2: Mount RightSidebarPanels in policy-editor.tsx right sidebar area</name>
  <files>apps/workbench/src/components/workbench/editor/policy-editor.tsx</files>
  <read_first>
    - apps/workbench/src/components/workbench/editor/policy-editor.tsx (from Task 1 modifications)
    - apps/workbench/src/components/workbench/editor/right-sidebar-panels.tsx (RightSidebarPanels props interface)
  </read_first>
  <action>
Import RightSidebarPanels and BuiltInPanel from "@/components/workbench/editor/right-sidebar-panels".

Add state: `rightSidebarPanelId` (string | null, default null).

The current right sidebar in policy-editor.tsx renders conditional 280px divs for: historyOpen (VersionHistoryPanel), evidenceOpen (EvidencePackPanel), explainOpen (ExplainabilityPanel), publishOpen (PublishPanel). These are at ~lines 997-1043.

Replace these with a single RightSidebarPanels component placed after the main editor `<div className="flex-1 min-w-0">` block. The RightSidebarPanels component returns a fragment containing [panel content div] + [icon strip div], so it fits naturally in the parent flex layout.

Construct builtInPanels array with useMemo:
- `{ id: "history", label: "Version History", icon: IconHistory, content: <VersionHistoryPanel policyId={versionDocumentId} currentYaml={state.yaml} currentPolicy={state.activePolicy} onRollback={handleRollback} onCompare={handleCompare} /> }`
- `{ id: "evidence", label: "Evidence Pack", icon: IconPackage, content: <EvidencePackPanel documentId={activeTab?.documentId} fileType={activeTab?.fileType} /> }`
- `{ id: "explainability", label: "Explainability", icon: IconBulb, content: <ExplainabilityPanel documentId={activeTab?.documentId} lastRun={labExecution.lastRun} baselineRun={labExecution.runHistory.length > 1 ? labExecution.runHistory[1] : null} onJumpToLine={(line) => window.dispatchEvent(new CustomEvent("workbench:jump-to-line", { detail: { line } }))} /> }`
- `{ id: "publish", label: "Publish", icon: IconFileExport, content: <PublishPanel documentId={activeTab?.documentId} fileType={activeTab?.fileType} source={state.yaml} validationValid={state.validation.valid && state.nativeValidation.valid !== false} lastLabRun={labExecution.lastRun} /> }`

Pass to RightSidebarPanels:
- `builtInPanels={builtInPanels}`
- `sidebarWidth={280}`
- `activePanelId={rightSidebarPanelId}`
- `onPanelChange={(id) => setRightSidebarPanelId(id)}`

Remove the individual conditional blocks for historyOpen, evidenceOpen, explainOpen, publishOpen and their corresponding toolbar toggle buttons' direct rendering logic. Update the toolbar buttons to set `rightSidebarPanelId` instead:
- historyOpen toggle -> `setRightSidebarPanelId(prev => prev === "history" ? null : "history")`
- evidenceOpen toggle -> `setRightSidebarPanelId(prev => prev === "evidence" ? null : "evidence")`
- explainOpen toggle -> `setRightSidebarPanelId(prev => prev === "explainability" ? null : "explainability")`
- publishOpen toggle -> `setRightSidebarPanelId(prev => prev === "publish" ? null : "publish")`

Remove the now-unused state variables: historyOpen, evidenceOpen, explainOpen, publishOpen (replaced by rightSidebarPanelId).

Render RightSidebarPanels at the end of the `<div className="flex-1 min-h-0 flex relative">` container (after ClaudeCodeHint), guarded by `!showHome`:
```tsx
{!showHome && (
  <RightSidebarPanels
    builtInPanels={builtInPanels}
    sidebarWidth={280}
    activePanelId={rightSidebarPanelId}
    onPanelChange={setRightSidebarPanelId}
  />
)}
```

NOTE: RightSidebarPanels returns a fragment with two elements (panel content + icon strip). The parent flex layout positions them correctly since they use shrink-0 and border-l styling.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/clawdstrike/.claude/worktrees/plugin-ecosystem && grep -n "RightSidebarPanels" apps/workbench/src/components/workbench/editor/policy-editor.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "import.*RightSidebarPanels" policy-editor.tsx returns a match
    - grep "builtInPanels" policy-editor.tsx returns at least one match
    - grep "<RightSidebarPanels" policy-editor.tsx returns at least one match
    - The RightSidebarPanels component receives builtInPanels, sidebarWidth, activePanelId, and onPanelChange props
  </acceptance_criteria>
  <done>RightSidebarPanels is imported and rendered in policy-editor.tsx with built-in panels (Version History, Evidence Pack, Explainability, Publish) passed as builtInPanels prop. The individual conditional sidebar divs are replaced by the unified RightSidebarPanels component. Plugin-contributed rightSidebarPanel views will automatically appear via the ViewRegistry hook inside RightSidebarPanels.</done>
</task>

<task type="auto">
  <name>Task 3: Embed PluginContextMenuItems in TabContextMenu</name>
  <files>apps/workbench/src/components/workbench/editor/policy-tab-bar.tsx</files>
  <read_first>
    - apps/workbench/src/components/workbench/editor/policy-tab-bar.tsx (TabContextMenu component ~line 32-105)
    - apps/workbench/src/components/plugins/plugin-context-menu.tsx (PluginContextMenuItems interface)
    - apps/workbench/src/lib/plugins/context-menu-registry.ts (ContextMenuTarget, WhenContext types)
  </read_first>
  <action>
Import PluginContextMenuItems from "@/components/plugins/plugin-context-menu".
Import type { WhenContext } from "@/lib/plugins/context-menu-registry".

In the TabContextMenu component (inner component of policy-tab-bar.tsx), after the existing menu items (Close, Close Others, Close All, separator, Duplicate, Rename), add PluginContextMenuItems:

```tsx
<PluginContextMenuItems
  menu="tab"
  context={{ tabId: menu.tabId } as WhenContext}
  onExecuteCommand={(commandId) => {
    console.log(`[TabContextMenu] Execute command: ${commandId}`);
    onClose();
  }}
/>
```

The `context` provides the tabId so when-clause predicates can reference it (e.g. `when: "tabId"`). The `onExecuteCommand` callback currently logs the command since there is no global command executor wired yet -- the critical thing is the integration point exists and the PluginContextMenuItems component is rendered.

Place the PluginContextMenuItems AFTER the last menu item (Rename) but still inside the context menu container div.

The PluginContextMenuItems component is headless -- it returns its own separator + button elements when there are visible items, and returns null when there are none. So it integrates seamlessly into the existing context menu.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/clawdstrike/.claude/worktrees/plugin-ecosystem && grep -n "PluginContextMenuItems" apps/workbench/src/components/workbench/editor/policy-tab-bar.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "import.*PluginContextMenuItems" policy-tab-bar.tsx returns a match
    - grep 'menu="tab"' policy-tab-bar.tsx returns a match
    - grep "onExecuteCommand" policy-tab-bar.tsx returns at least one match
    - PluginContextMenuItems is rendered inside the TabContextMenu component's container div
  </acceptance_criteria>
  <done>PluginContextMenuItems is imported and rendered in the TabContextMenu component with menu="tab", a WhenContext containing the tabId, and an onExecuteCommand handler. Plugin context menu items registered for the "tab" menu target will appear in the tab right-click context menu.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -c "BottomPanelTabs" apps/workbench/src/components/workbench/editor/policy-editor.tsx` returns >= 2 (import + render)
2. `grep -c "RightSidebarPanels" apps/workbench/src/components/workbench/editor/policy-editor.tsx` returns >= 2 (import + render)
3. `grep -c "PluginContextMenuItems" apps/workbench/src/components/workbench/editor/policy-tab-bar.tsx` returns >= 2 (import + render)
4. `npx vitest run --reporter=verbose 2>&1 | tail -20` -- existing tests still pass
</verification>

<success_criteria>
- BottomPanelTabs is mounted in policy-editor.tsx with built-in tabs passed as props
- RightSidebarPanels is mounted in policy-editor.tsx with built-in panels passed as props
- PluginContextMenuItems is embedded in TabContextMenu in policy-tab-bar.tsx
- All existing vitest tests pass (no regressions)
- The 3 previously-orphaned components are now reachable in the live app layout
</success_criteria>

<output>
After completion, create `.planning/phases/05-layout-wiring-sdk-injection/05-01-SUMMARY.md`
</output>
