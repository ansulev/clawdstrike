---
milestone: v4.0
audited: 2026-03-21
status: tech_debt
scores:
  requirements: 33/33
  phases: 5/5
  integration: 38/39
  flows: 3/5
gaps:
  requirements: []
  integration:
    - id: "TRANSLATION_UI"
      status: "tech_debt"
      affected_requirements: ["SPL-06", "KQL-06", "EQL-06", "YARAL-06", "CORE-03"]
      evidence: "Translation providers registered and discoverable via getTranslationPath(), but no UI component invokes provider.translate() — translations only reachable as publish targets via adapter.buildPublication()"
    - id: "HUB_AND_SPOKE_ROUTING"
      status: "tech_debt"
      affected_requirements: []
      evidence: "Two-hop translation (SPL->Sigma->KQL) requires an orchestrator that chains two getTranslationPath() calls — no such function exists yet"
  flows:
    - id: "TRANSLATE_FORMAT"
      status: "broken_at_ui"
      evidence: "Flow C (translate Sigma to/from any format): provider infrastructure complete, no UI trigger"
    - id: "CROSS_FORMAT_HUB"
      status: "broken_at_orchestration"
      evidence: "Flow E (hub-and-spoke SPL->Sigma->KQL): no two-hop routing logic exists"
tech_debt:
  - phase: 01-core-detection-plugin-infrastructure
    items:
      - "CORE-08: DetectionVisualPanelKit exists but 3 of 4 new adapter panels import from shared-form-fields directly instead of through the kit"
  - phase: cross-phase
    items:
      - "No UI surface calls getTranslationPath() + provider.translate() — translation providers are registered but only consumed for publish target enumeration"
      - "Hub-and-spoke cross-format routing needs a chainTranslation(from, to) orchestrator that resolves multi-hop paths through Sigma"
      - "yaral-translation.ts duplicates parseYaralRule from yaral-adapter.ts (parser not exported from adapter)"
---

# v4.0 Detection Adapter Plugins — Milestone Audit

## Requirements Coverage: 33/33

| Status | Count | Details |
|--------|-------|---------|
| Satisfied | 33 | All requirements met at infrastructure level |
| Partial | 0 | — |
| Unsatisfied | 0 | — |

### Requirements by Phase

| Phase | Requirements | Status |
|-------|-------------|--------|
| Phase 1: Core Infrastructure | CORE-01 through CORE-09 (9) | All satisfied |
| Phase 2: SPL Adapter | SPL-01 through SPL-06 (6) | All satisfied |
| Phase 3: KQL Adapter | KQL-01 through KQL-06 (6) | All satisfied |
| Phase 4: EQL Adapter | EQL-01 through EQL-06 (6) | All satisfied |
| Phase 5: YARA-L Adapter | YARAL-01 through YARAL-06 (6) | All satisfied |

### Detailed Requirement Evidence

| Requirement | Evidence |
|-------------|----------|
| CORE-01 | `unregisterAdapter()` in adapters.ts returns void, matches dispose pattern |
| CORE-02 | `registerVisualPanel/getVisualPanel` in visual-panels.ts, dynamic resolution in split-editor.tsx |
| CORE-03 | `registerTranslationProvider/getTranslationPath` in translations.ts, 4 providers registered |
| CORE-04 | `TranslationProvider` interface with `canTranslate/translate`, `TranslationResult` includes all required fields |
| CORE-05 | 53-entry field mapping table in field-mappings.ts with `registerFieldMappings()` for extension |
| CORE-06 | `PublishTarget` is `string`, `BUILTIN_PUBLISH_TARGETS` const, dynamic registry |
| CORE-07 | `DetectionVisualPanelProps` with source/onSourceChange/readOnly/accentColor, 7 panels migrated |
| CORE-08 | `detection-panel-kit.tsx` exports 8 components (Section, FieldLabel, TextInput, TextArea, SelectInput, SeverityBadge, AttackTagBadge, FieldMappingTable) |
| CORE-09 | `DetectionAdapterContribution` in types.ts bundles fileType/entrypoint/fileTypeDescriptor/hasVisualPanel/translations |
| SPL-01 | `splunk_spl` registered with `.spl`, `#65a637`, content detection, starter template |
| SPL-02 | `canDraftFrom()` for process/file/network/registry; `buildDraft()` generates SPL with CIM fields |
| SPL-03 | `runLab()` with `matchSplConditions`, `plugin_trace` with `spl_match` traceType |
| SPL-04 | `buildPublication()` supports `"spl"` and `"json_export"` targets |
| SPL-05 | `SplVisualPanel` pipe-chain builder with editable command cards, self-registered |
| SPL-06 | `splTranslationProvider` declares `sigma_rule↔splunk_spl`, implements `canTranslate/translate` |
| KQL-01 | `kql_rule` registered with `.kql`, `#0078d4`, content detection, starter template |
| KQL-02 | `canDraftFrom()` for process/file/network/auth; `buildDraft()` with Sentinel tables |
| KQL-03 | `runLab()` with `clientSideKqlMatch`, `plugin_trace` with `kql_where_match` traceType |
| KQL-04 | `buildPublication()` supports `"kql"` and `"json_export"` (Analytics Rule JSON) |
| KQL-05 | `KqlVisualPanel` tabular expression editor with table selector, where-clause cards |
| KQL-06 | `kqlTranslationProvider` declares `sigma_rule↔kql_rule`, bidirectional |
| EQL-01 | `eql_rule` registered with `.eql`, `#f04e98`, content detection (`process where`, `sequence by`) |
| EQL-02 | `canDraftFrom()` for process/file/network/registry + multi-event; ECS field names |
| EQL-03 | `runLab()` with `matchSingleQuery/matchSequenceQuery`, per-step `eql_sequence_match` traces |
| EQL-04 | `buildPublication()` supports `"eql"` and `"json_export"` (NDJSON detection rule) |
| EQL-05 | `EqlVisualPanel` dual-mode: SingleQueryEditor + SequenceBuilder with reorderable steps |
| EQL-06 | `eqlTranslationProvider` declares `sigma_rule↔eql_rule`, sequence→Sigma degrades with untranslatableFeatures |
| YARAL-01 | `yaral_rule` registered with `.yaral`, `#4285f4`, content detection (`rule {`, `events:`) |
| YARAL-02 | `canDraftFrom()` for process/file/network/auth; UDM field paths, meta/events/condition structure |
| YARAL-03 | `runLab()` with per-variable matching, `plugin_trace` with `yaral_match` traceType |
| YARAL-04 | `buildPublication()` supports `"yaral"` and `"json_export"` (Chronicle metadata) |
| YARAL-05 | `YaralVisualPanel` with meta editor, event variable cards ($e1/$e2), condition editor |
| YARAL-06 | `yaralTranslationProvider` declares `sigma_rule↔yaral_rule`, multi-event degrades with untranslatableFeatures |

## Integration: 38/39 connections wired

### Wired (38)
- 4 adapters → Phase 1 registries (registerAdapter, registerFileType, translateField)
- 4 translation providers → Phase 1 translation registry (registerTranslationProvider)
- 4 visual panels → Phase 1 visual panel registry (registerVisualPanel)
- 7 visual panels → split-editor.tsx side-effect imports (sigma, yara, ocsf + 4 new)
- 4 adapters → field-mappings.ts translateField
- 4 translation providers → field-mappings.ts getAllFieldMappings (reverse mapping)
- Barrel index.ts → all 8 modules (4 adapters + 4 translation providers) exported + side-effect imported
- Plugin loader → detectionAdapters contribution routing
- use-publication.ts → getTranslatableTargets for publish target dropdown
- SDK types.ts → DetectionAdapterContribution interface

### Missing (1)
- No UI component invokes `getTranslationPath()` + `provider.translate()` for explicit format-to-format translation

## E2E Flows: 3/5

| Flow | Status |
|------|--------|
| A. Create new file (any format → icon + visual panel) | Complete |
| B. Run Lab (any format → adapter.runLab → plugin_trace) | Complete |
| C. Translate Sigma ↔ format (translation provider) | Broken at UI invocation |
| D. Publish target enumeration (format → available targets) | Complete |
| E. Hub-and-spoke (SPL → Sigma → KQL) | Broken at orchestration |

## Tech Debt

| Phase | Item |
|-------|------|
| Phase 1 | CORE-08: 3 of 4 new panels import from shared-form-fields directly instead of detection-panel-kit |
| Cross-phase | No UI calls getTranslationPath() + provider.translate() — translation infrastructure unused at UI layer |
| Cross-phase | Hub-and-spoke needs chainTranslation() orchestrator for multi-hop routing |
| Phase 5 | yaral-translation.ts duplicates parseYaralRule from yaral-adapter.ts (not exported) |
